*, *::before, *::after {
  box-sizing: border-box;
}

html, body, .container {
  width: 100%;
  height: 100%;
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  font-size: 16px;
  line-height: 1.5em;
  color: #333;
}

.container {
  background: #ecf0f1;
  position: relative;
}
